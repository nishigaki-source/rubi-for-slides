# Googleスライド ルビ振り Chrome拡張 — 要件・システム要件・開発計画

作成日: 2026-09-17

---

## 1. 目的とゴール

Googleスライド上の日本語テキストに含まれる漢字に、ひらがなのルビ（ふりがな）を付ける Chrome 拡張機能を作る。

### 最重要の設計判断: 「誰のためのルビか」

「ルビをふる」には性質の異なる 2 つの意味があり、実装も権限もまったく変わる。

| モード | 何をするか | 誰に見えるか | 認証 | 難易度 |
|---|---|---|---|---|
| A. 表示オーバーレイ | 画面上の描画にルビを重ねて表示する。スライドの内容は変更しない | 拡張を入れた本人だけ | 不要 | 中 |
| B. スライドへ書き込み | 漢字の真上に小さなテキストボックスを生成し、実データとしてルビを埋め込む | 共有相手・PDF/PPTX書き出し・発表時の全員 | Google OAuth（Slides API） | 高 |

教材づくり（児童・生徒・日本語学習者に配布する）用途なら B が必須。自分が読むための補助なら A で足りる。
本計画は **A を MVP として先行し、B を第 2 段階として積み上げる** 構成にする。A で作る「文字ごとの座標取得」「読みの生成と整列」は B でもそのまま使うため、順序として無駄がない。

---

## 2. 要件整理

### 2.1 機能要件（Must）

1. スライド編集画面で、テキスト中の漢字を検出し、ひらがなの読みをルビとして表示する。
2. ルビの表示 ON/OFF をワンクリックで切り替えられる。
3. 送り仮名を正しく除外する（例: 「食べる」→「食」にだけ「た」）。
4. 熟語はまとまりで読みを付ける（グループルビ）。例: 「学校」→「がっこう」。
5. 読みの生成はブラウザ内で完結させ、スライド本文を外部サーバへ送らない（学校・企業利用を想定した前提）。
6. ルビのフォントサイズは本文比で調整できる（既定 50%）。
7. 編集・スクロール・ズーム・スライド切り替えに追従して表示が崩れない。

### 2.2 機能要件（Should）

8. 学年別フィルタ: 「小学 N 年までに習う漢字にはルビを付けない」を選べる（学年別漢字配当表を同梱）。
9. ユーザー辞書: 固有名詞・人名・専門用語など誤読しやすい語の読みを登録・上書きできる。
10. プレゼンテーション（発表）モードでも表示できる。
11. ルビの見た目（フォント・サイズ・色）を全体設定として変更できる（サイズは実装済み）。
12. **単語ごとの見た目の個別上書き**（2026-09-17 決定）: ユーザー辞書のエントリごとに、読みだけでなくフォント・色・サイズ比も個別に指定できる。指定が無い項目は全体設定にフォールバックする。
13. 【モード B】現在のスライド / 全スライドに対してルビをテキストボックスとして書き込む。
14. 【モード B】書き込んだルビを一括で削除できる（元に戻せる）。
15. 【モード B】ルビと元のテキストボックスをグループ化し、移動時に一緒に動くようにする。
16. 【モード B】印刷・PDF/PPTX書き出しにルビを反映させたい場合は、モード B（実データとしての書き込み）が必須（2026-09-17 決定、3.6節参照）。

### 2.3 機能要件（Could / 将来）

17. モノルビ（漢字 1 文字ずつに読みを割り当てる）。
18. 縦書きテキスト対応。
19. 読みの候補が複数ある語で、クリックして選び直せる。
20. Google ドキュメントへの横展開。

### 2.4 非機能要件

- 対応ブラウザ: Chrome 最新 2 メジャーバージョン（Manifest V3）。Edge も同一パッケージで動作すること。
- 初回の辞書ロードは 3 秒以内、以降は即時（メモリキャッシュ）。
- 1 スライド（テキスト 500 文字程度）のルビ描画は 100ms 以内。
- Google 側の DOM 変更で壊れたとき、拡張が例外を投げてスライド編集を妨げないこと（フェイルセーフに「何も表示しない」）。
- 個人情報の外部送信なし。モード B の API 通信先は Google のみ。
- UI は日本語（i18n 基盤は用意し英語も追加可能に）。

### 2.5 対象外（スコープから明示的に外す）

- Google スライド以外のサイト。
- 画像内の文字（OCR）。
- 音声読み上げ。
- 中国語・韓国語の漢字。

---

## 3. システム要件

### 3.1 技術スタック

| 領域 | 選定 | 理由 |
|---|---|---|
| 拡張仕様 | Manifest V3 | 現行の唯一の選択肢。リモートコード実行不可のため辞書は同梱 |
| 言語 | TypeScript | 型安全。整列ロジックのテストが書きやすい |
| ビルド | Vite + CRXJS（または esbuild） | MV3 のマルチエントリ（content / worker / popup）を扱いやすい |
| 形態素解析 | kuromoji.js（IPADIC）を同梱 | 純 JS でオフライン動作。読み（カタカナ）を返す。辞書は十数 MB |
| カナ変換 | 自前（カタカナ→ひらがなはコードポイント −0x60） | 依存を増やさない |
| 学年別漢字 | 公開の学年別漢字配当表を JSON 化して同梱 | 静的データ |
| 設定保存 | chrome.storage.sync | 端末間で共有 |
| 単体テスト | Vitest | 整列・辞書・座標変換ロジック |
| 結合テスト | Playwright + Slides の SVG を模した固定 HTML | 本物の Slides はログインが必要で CI 不向き |
| 【B】API | Google Slides REST API `presentations.batchUpdate` | 公式手段。chrome.identity で OAuth |

Sudachi（WASM）は精度が上だが配布サイズと初期化が重い。まず kuromoji で作り、精度不足が明らかになった段階で差し替え可能なインターフェースにしておく。

### 3.2 権限（manifest）

- 共通: `storage`、host permission `https://docs.google.com/presentation/*`
- モード B 追加: `identity`、`oauth2.client_id`、scope `https://www.googleapis.com/auth/presentations`

`presentations` は sensitive scope のため、Chrome ウェブストアで公開配布するなら Google の OAuth 審査が必要（数週間かかる）。社内・校内配布（限定公開）なら未審査でも利用可能だが警告画面が出る。

### 3.3 アーキテクチャ

```
docs.google.com/presentation/d/<id>/edit
 ├─ content script
 │    ├─ DomWatcher      : MutationObserver + rAF でスライド SVG の変化を検知
 │    ├─ TextExtractor   : SVG <text> からテキストと文字ごとの座標を取得
 │    │                     (SVGTextContentElement.getExtentOfChar を利用)
 │    ├─ OverlayRenderer : 取得座標をもとにルビ層を描画 (モード A)
 │    ├─ CoordMapper     : 画面 px → スライド EMU への変換 (モード B)
 │    └─ Injector        : Slides API 用の batchUpdate リクエストを組み立て
 │
 ├─ service worker
 │    ├─ Tokenizer host  : kuromoji 辞書を fetch(chrome.runtime.getURL) でロード
 │    │                     (XHR 非対応のため kuromoji の loader を差し替え)
 │    ├─ ReadingService  : 形態素 → 読み → 送り仮名分離 → 学年フィルタ → ユーザー辞書
 │    └─ SlidesClient    : OAuth トークン取得と batchUpdate 実行 (モード B)
 │
 └─ popup / options
      ├─ ON/OFF、サイズ比、学年フィルタ
      ├─ ユーザー辞書の編集
      └─ 【B】このスライド / 全スライドに書き込む、ルビをすべて削除
```

メッセージは content → worker の一方向リクエスト/レスポンス（`chrome.runtime.sendMessage`）で統一する。worker はテキスト配列を受け取り、`[{surface, base, reading, rubyRanges:[{start,end,kana}]}]` を返す。DOM 依存は content 側だけに閉じ込める。

### 3.4 読み生成の中核ロジック（両モード共通）

1. kuromoji でトークン化し、`reading`（カタカナ）を取得する。読みが無いトークン（記号・未知語）はスキップ。
2. カタカナ → ひらがな変換。
3. 送り仮名の分離: 表層形の末尾/先頭にある仮名と読みの末尾/先頭を突き合わせ、一致部分を取り除く。残った漢字部分に残った読みを割り当てる（例: 食べる / たべる → 食=た）。
4. 漢字が含まれないトークンはルビ対象外。英数字・記号も対象外。
5. 学年フィルタ: トークン内の漢字がすべて指定学年以下なら対象外。
6. ユーザー辞書が表層形に一致すれば読みを上書き。
7. 漢字と仮名が交互に現れる語（例: 「打ち合わせ」）は仮名の位置で分割し、区間ごとに割り当てる。割り当てが一意に決まらない場合はグループルビにフォールバック。

このロジックは DOM に依存しないので、単体テストを厚く書く。

### 3.5 モード B の書き込み方式

Slides にはルビ機能がないため、次の手順で「小さなテキストボックス」として埋め込む。

**Phase 0 での検証を踏まえた確定方針**: DOM のシェイプ id（`editor-i0` 等）は Slides API の `objectId` と一致しないため、両者は「位置＋テキスト内容」で突き合わせる。また px↔EMU 変換は SVG の内部 viewBox 値を使わない（値の意味が確証できないため）。

1. Slides API `presentations.get` でページ内の全シェイプの `objectId`・位置（EMU）・テキスト内容を取得する。
2. エディタのページ要素の実測ピクセル幅と、手順 1 で得たページ幅（EMU）の比率から px → EMU のスケール係数をその場で算出する。
3. content script が画面上の各漢字区間の bounding box（px、`getExtentOfChar` + `getScreenCTM` で取得）を、この係数で EMU に変換する。
4. 各 API シェイプの EMU 位置と DOM 側の bounding box を近さで対応付け、候補が複数あればテキスト内容の完全一致で絞り込む。一意に決まらない場合はそのシェイプへの書き込みをスキップし、ユーザーに確認を促す。
5. `createShape`（TEXT_BOX）+ `insertText` + `updateTextStyle`（フォントサイズ = 本文 × 比率、行間 100%、余白 0）+ `updateShapeProperties`（autofit なし）を 1 回の `batchUpdate` にまとめる。
6. 生成したシェイプの `description`（代替テキスト）に `rubi:v1:<元objectId>` を書いておき、後で一括削除・再生成の対象を特定する。
7. 元シェイプとルビ群を `groupObjects` でグループ化する（設定で ON/OFF）。
8. ルビの行が前の行と重なる場合に備え、「行間を 150% に広げる」オプションを用意する（`updateParagraphStyle`）。

制約: 書き込み後に本文を編集してもルビは追従しない。「削除 → 再生成」のワークフローを UI で明示する。

### 3.6 印刷・PDF書き出しへの対応（2026-09-17 決定・重要な制約）

**モード A（表示オーバーレイ）は、その仕組み上、印刷・PDF/PPTX書き出しに絶対に反映されない。**

モード A は `position:fixed` の DOM 要素をブラウザの画面上に重ねて表示しているだけで、スライドのデータそのものには一切手を加えない。Google スライドの印刷・PDF/PPTX 書き出しは、拡張機能が画面に追加した DOM を経由せず、Google 側がドキュメントの内部データから直接描画する。したがって、モード A だけではルビが印刷物や PDF に現れることは原理的にあり得ない。

印刷・PDF/PPTX 書き出しにルビを反映させたい場合は、**モード B（3.5節、実際のテキストボックスとしてスライドに書き込む）が必須**である。モード B で書き込んだルビは本物のスライドの内容になるため、印刷・PDF・共有相手の画面・発表モードのすべてに反映される。

この制約はユーザーに明確に伝える(popup や README で「画面表示のみ。印刷やPDFに出したい場合は書き込み機能を使ってください」等の案内を出す)。

### 3.7 単語ごとの見た目の個別上書き（2026-09-17 決定）

ユーザー辞書は当初「表層形 → 読み」の単純な対応表として設計したが、フォント・色・サイズを単語ごとに個別指定したいという要望を受け、次の形に拡張する。

```ts
interface UserDictEntry {
  reading?: string;   // ひらがなの読み上書き(省略時はkuromojiの読みをそのまま使う)
  style?: {
    fontFamily?: string;
    color?: string;
    sizeRatio?: number; // 全体設定のsizeRatioをこの単語だけ上書きする
  };
}
type UserDictionary = Record<string, UserDictEntry>;
```

- 全体設定(popup のフォント・色・サイズ)は「既定値」として機能し、`style` に個別指定が無い項目はそこにフォールバックする。
- `reading` と `style` は独立に指定できる(読みだけ直したい、見た目だけ変えたい、の両方に対応)。
- **既存のコード(`src/core/{types,reading,userDict}.ts`、`src/shared/userDictStorage.ts`)は `UserDictionary = Record<string, string>` という単純な形で実装済みのため、この拡張には破壊的なデータ構造変更が必要。** Phase 1 の時点では実データがほぼ無い(options UI 未実装で誰も辞書エントリを保存していない)ため、今のうちに移行するのが低コスト。Phase 3 で options UI を作る際にあわせて実施する。
- `overlayRenderer.ts` の `createRubySpan` は現在 `font-family: sans-serif` と `color: #1a1a1a` を固定値としているため、これを「全体設定 + 単語ごとの上書き」から算出した値に差し替える必要がある。

### 3.8 発表モードは対応不可能と判明（2026-09-17 発見・重要な制約）

Phase 1 完了後の実機検証で、発表モード(`/presentation/d/<id>/present`)の DOM 構造を調査した結果、次が判明した。

- 編集画面: 文字は 1 つずつ `<text>` 要素として描画される(PHASE0_FINDINGS.md 1節、本ファイル 3.3節)。
- **発表モード: `<text>` 要素が 0 個。文字はすべて `<path>`(フォントの輪郭をベクターパス化した図形)として描画される。**

これは Google 側が発表・閲覧用に別のレンダリングパイプラインを使っているためと考えられる(フォントの一貫した表示やライセンス上の理由が推測される)。`<path>` には「どの文字を表しているか」という情報が一切残らないため、文字を検出してオーバーレイを重ねるモード A の仕組みは、発表モードでは**原理的に実現不可能**と判断した。

**対応方針**:
- モード A(表示オーバーレイ)は「編集画面限定の機能」として明確に位置づけ直す。要件 2.2 の項目10「プレゼンテーション（発表）モードでも表示できる」は、**モード A では達成できない**ことをユーザーに明示し、モード B の前提知識として扱う。
- モード B(3.5節)で書き込んだルビは実際のスライドコンテンツになるため、発表モードのレンダラーがそれをどう描画するか(パス化されるかどうか)に関わらず、本文と同じように正しく表示されるはずである(本文自体が発表モードでパス化されても視覚的に正しく表示されていることから推測できる。Phase 2 実装後に実機で要検証)。
- popup や README に「ルビの画面表示は編集画面でのみ有効です。発表時や印刷にも出したい場合は書き込み機能をお使いください」という趣旨の案内を追加する(Phase 1 残タスクとして追記)。

---

## 4. 主要リスクと対策

| リスク | 影響 | 対策 |
|---|---|---|
| Google が内部 DOM/クラス名を変更する | 表示が消える | セレクタを 1 ファイルに集約。構造的特徴（SVG 内の `<text>`）で探索し、クラス名依存を最小化。壊れたら黙って無効化しユーザーに通知 |
| kuromoji の誤読（人名・固有名詞・数詞） | 誤ったルビ | ユーザー辞書。将来 Sudachi 差し替え可能な Tokenizer インターフェース |
| 辞書サイズと初回ロード時間 | UX 低下 | 遅延ロード、メモリキャッシュ、ロード中インジケータ |
| 【B】OAuth 審査 | 公開が遅れる | Phase 1 の段階で GCP プロジェクトと審査申請を先に始める |
| 【B】文字座標が画面依存で微妙にずれる | ルビ位置ズレ | 位置は EMU で丸め、サイズ比とオフセットを設定可能に。プレビューしてから書き込み |
| 回転・グループ化・表内テキスト | 座標変換が複雑 | Phase 1 は非回転の通常テキストボックスに限定。表・回転は Phase 3 で対応 |
| 閲覧専用のスライド | 書き込み不可 | モード A のみ有効化し、B のボタンを無効表示 |
| **【新規発見・2026-09-17】発表モードは `<text>` 要素が存在せず、文字がすべて `<path>`(アウトライン化された図形)として描画される | モード A は発表モードで原理的に動作しない | **設計上回避不可能と判断。** モード A は編集画面限定の機能として明確化し、発表モードでルビを見せたい場合はモード B(書き込み)を案内する。3.8節参照 |

---

## 5. 開発計画

### Phase 0: 技術検証（1 週間）— ✅ 完了（2026-09-17、結論: Go）

目的: 「作れるか」を確定する。ここで出た結果次第で Phase 1 以降の設計を修正する。

詳細な検証結果は [PHASE0_FINDINGS.md](PHASE0_FINDINGS.md) を参照。

- [x] Slides 編集画面の SVG 構造を調べ、テキスト要素の探索方法を確定する → **1 文字 1 `<text>` 要素**という、想定より扱いやすい構造と判明
- [x] `getExtentOfChar` で文字ごとの座標が取れるか、ズーム・スクロール後も正しいかを確認する → `getExtentOfChar` と `getScreenCTM` の組み合わせで実ピクセル位置を誤差 0.01px 未満で取得できることを実機で確認
- [x] MV3 service worker 上で kuromoji の辞書を fetch でロードできるか確認する（loader 差し替えの検証）→ XMLHttpRequest 未定義環境を Node 上で再現し、fetch ベースの薄いシムで無改変ロードに成功することを実証（639ms）
- [x] 【B】DOM 上の要素と Slides API の `objectId` を対応付けられるか確認する → **DOM の id（`editor-i0` 等）は API の objectId と一致しないことが判明**。位置＋テキスト内容によるマッチング方式に設計変更（3.3・3.5節に反映済み）
- [x] 【B】px → EMU 変換の精度を 1 枚のスライドで実測する → SVG 内部座標（viewBox）は解読不能な独自単位と判明。API のページサイズと実測ピクセル幅から都度スケール係数を算出する方式に変更

成果物: [PHASE0_FINDINGS.md](PHASE0_FINDINGS.md)、Go / No-Go 判断（Go）。Phase 1 のビルド設定に Node コアモジュール（`path`）のポリフィル追加が必要と判明したため、Phase 1 のタスクに反映済み。

### Phase 1: MVP — 表示オーバーレイ（2〜3 週間）

- [x] プロジェクト雛形（Vite + TypeScript + manifest v3、lint、Vitest）✅ 完了（2026-09-17）。`vite-plugin-node-polyfills` で kuromoji 用の `path` 解決を追加済み。`npm run build` / `npm run test` / `npm run lint` すべて成功を確認
- [x] ReadingService 実装と単体テスト ✅ 完了（2026-09-17）。`src/core/{types,kana,reading,gradeFilter,userDict}.ts`、単体テスト 48件パス。詳細は [README.md](README.md) を参照。学年別漢字配当表は `public/data/kanji-grades.sample.json` にサンプル/プレースホルダーを用意（**公式データへの差し替えが必要**、機能自体は動作確認済み）
- [x] Tokenizer host ✅ 完了（2026-09-17）。`src/worker/{xhrShim,tokenizer,gradeTable,index}.ts`。kuromoji 辞書(`public/dict/`)・学年別漢字配当表(`public/data/`)を拡張機能にバンドルし、content ⇔ worker のメッセージプロトコル(`src/core/messages.ts`)経由でトークン化・学年別漢字配当表取得を提供
- [x] TextExtractor / OverlayRenderer ✅ 完了・実スライドで動作確認済み（2026-09-17）。`src/content/{selectors,textExtractor,overlayRenderer,geometry,rubyLayout,rubyPipeline,tokenizeClient,gradeTableClient}.ts`。ユーザー提供の実サンプル（「はじめての麻雀プログラミング」全12枚）で確認し、実機テストでのみ判明する不具合を複数発見・修正した:
  - **1文字1`<text>`要素という前提が崩れるケースがある**（同じ書式の連続文字が1要素にまとまる例:「テッ」「う！」）。文字ごとに `charIndexInElement` を持たせ、複数文字要素では `getExtentOfChar`+`getScreenCTM` で個別位置を取得するよう修正
  - **改行をまたぐルビの位置がおかしい**問題を、行ごとのグループ化(`groupIndicesByLine`)で解消
  - **スピーカーノート欄のプレースホルダーにもルビが付く**問題を、`speakernotes-` prefix での除外で解消
  - **隣接する語のルビ同士が重なって文字が隠れる**問題(例:「神経」+「衰弱」で「しんけいすいじゃく」が「しんすいじゃく」に見えた)を発見。`rubyLayout.ts` の `computeRubyFontSize` で、ルビの文字数に応じてフォントサイズを自動調整するよう修正(実機での複数回の調整を経て、最小可読サイズ8pxを保証しつつ隣接語との重なりを最小化するバランスに設定)
  - 汎用辞書に無い麻雀用語(「筒子」「雀頭」等)は機械的な誤読になることを確認。バグではなく、ユーザー辞書(Phase 3)で対応する想定内の制約
- [x] DomWatcher ✅ 完了（2026-09-17）。`src/content/domWatcher.ts`。MutationObserver(debounce 250ms) + resize/scroll 監視。**実装中に発見した重要な不具合を修正済み**: オーバーレイ自身の DOM 変更を監視対象に含めると「再描画→mutation検知→再描画」の無限ループになるため、オーバーレイ配下の変更は無視するフィルタを追加した
- [x] popup: ON/OFF、サイズ比、学年フィルタ ✅ 完了（2026-09-17）。`src/popup/{index.html,main.ts}`。`chrome.storage.sync` 経由で content script とリアルタイムに同期
- [x] 発表モード対応 — ✅ 検証完了・**対応不可能と判明**（2026-09-17）。実機調査の結果、発表モードは文字を `<text>` 要素ではなく `<path>`(アウトライン化された図形)として描画しており、モード A は原理的に動作しない。詳細は 3.8節を参照。要件 2.2-10 を「モード B でのみ達成可能」に修正済み
- [x] popup/README に「ルビの画面表示は編集画面限定。発表・印刷にも出したい場合は書き込み機能(Phase 2)を」という案内を追加 ✅ 完了（2026-09-17、v0.1.10）
- [x] 固定 HTML による Playwright テスト ✅ 完了（2026-09-17）。`tests/e2e/fixtures/slides-like.html`(実機で確認した Slides の DOM 構造を再現)+ `tests/e2e/rubi.spec.ts`。content script(`src/content/index.ts`)本体を `vite.e2e.config.ts` でバンドルし、`chrome.*` API だけを本物の kuromoji で裏打ちしたスタブに差し替えて実ブラウザ(Chromium)上で検証する構成。表示・実機で発見した2つの不具合の回帰(複数文字要素での位置ズレ、改行またぎ)・サムネイル/スピーカーノート除外・DomWatcherによる編集追従・ON/OFF切り替えの計7件がすべてパス。テストが実際に不具合を検出できることも、意図的に不具合を再現したコードで一度失敗させて確認済み(サニティチェック)。`npm run test:e2e` で実行
- [x] 社内・校内向けにローカル読み込み（デベロッパーモード）で試用開始 ✅ 完了（2026-09-17）。ユーザーの実 Chrome 環境に `chrome://extensions` の「パッケージ化されていない拡張機能を読み込む」から読み込み、実際のサンプルプレゼンテーション（全12枚）で動作確認した

**残っている既知の軽微な課題**（致命的ではなく、Phase 3 以降で改善検討）:
- 本文が1行目にあり上に十分な余白が無い非常に大きいフォントのタイトルで、ルビがテキストボックスの外(背景側)にわずかにはみ出ることがある
- 本文フォントサイズが極端に小さいスライドで、5文字以上の長い読みが2文字幅の狭い本文に乗る場合、隣接語のルビとわずかに重なることがある（`sizeRatio` を下げることで軽減可能）
- 隣接する2語のルビの間に少し余白があった方が読みやすい（現状は文字が欠けることはないが、間隔が詰まって見える場合がある）

完了条件: 自分のスライドを開いてルビが表示され、編集しても追従し、OFF にすると消える。**✅ 達成（2026-09-17）**。ユーザーの実 Chrome 環境・実サンプルプレゼンテーションでの確認、および Playwright による自動 e2e テスト(7件)の両方でカバー済み。

**Phase 1 は全タスク完了。** 次は Phase 2(モード B)に進む。

### Phase 2: スライドへの書き込み（2〜3 週間）

印刷・PDF/PPTX書き出しにルビを反映させるための必須フェーズ（3.6節参照。モード A だけでは原理的に不可能）。

- [x] GCP プロジェクト作成、OAuth クライアント（Chrome 拡張種別）、同意画面 ✅ 完了（2026-09-17）。プロジェクト名は英数字のみ許可(日本語不可)、アプリ名に "Google" を含めると Google の規約違反でエラーになる制約を実機で発見し対応。同意画面のテストユーザー登録・`presentations` スコープ追加・OAuthクライアントID発行まで完了。拡張機能IDは固定済み（`manifest.json` の `key` フィールド、`.secrets/` に秘密鍵を保管）。ウェブストア公開時の審査申請は別途必要
- [x] chrome.identity によるトークン取得と失効処理 ✅ 完了・実機で OAuth 認可フロー成功を確認（2026-09-17）。`src/worker/auth.ts`。401時の自動トークン失効・再試行にも対応
- [x] CoordMapper（px → EMU）✅ 完了・実機動作確認済み（2026-09-17）。`src/core/emu.ts`（純粋関数、単体テスト7件）。Phase 0 で確定した方式どおり、SVG 内部座標は使わず API のページサイズ(EMU)と実測ピクセル幅の比率からスケール係数を都度算出する
- [x] DOM シェイプ ⇔ API objectId の対応付け ✅ 完了（2026-09-17）。`src/core/shapeMatcher.ts`（純粋関数、単体テスト6件）。位置の近さ→テキスト内容一致の順でマッチングし、一意に決まらない場合は `undefined` を返す(グループ化のみスキップ。書き込み自体は続行する設計に変更 — 元の「書き込みごとスキップ」よりユーザー体験が良いと判断)
- [x] Injector: createShape / insertText / updateTextStyle / groupObjects の batchUpdate 生成 ✅ 完了・実機動作確認済み（2026-09-17）。`src/core/slidesRequests.ts`（純粋関数、単体テスト13件）。`src/worker/slidesClient.ts` が実際の HTTP 呼び出しを担当
- [x] 「このスライド」への適用 ✅ **完了・実機で書き込み成功を確認**（2026-09-17）。`src/content/writeController.ts`。実際のプレゼンテーションに4件のルビ用テキストボックスが正しい位置に作成されたことを DOM 調査で確認済み
- [x] 「全スライド」への適用 ✅ **完了・実機で12枚構成のプレゼンテーション全体への書き込み成功を確認**（2026-09-17）。`src/content/slideNavigator.ts` の `navigateToSlide` がフィルムストリップのサムネイル(`data-slide-page-id`)へ合成クリックを発行してページを自動切り替えし、`writeController.ts` の `writeRubyToAllSlides` が各ページで測定・書き込みを繰り返す。完了後は元のスライドへ自動復帰する
- [x] 書き込み成功後にモード A(画面表示)を自動 OFF ✅ 完了（2026-09-17）。`src/content/index.ts` の `disableDisplayModeAfterWrite`。書き込んだテキストボックスとオーバーレイが同時に存在すると、レンダリング方式の違いで二重表示に見える問題への対処
- [x] `description` マーカーによる一括削除 ✅ **完了・実機で削除成功を確認**（2026-09-17）。`src/core/slidesRequests.ts` の `buildDescription`/`isRubyDescription`/`extractMatchedShapeObjectId`。書き込んだ4件すべてが正しく削除され元の状態に戻ることを確認済み
- [ ] 行間拡張オプション — 未着手（優先度低。必要になった時点で `updateParagraphStyle` に追加）
- [ ] 書き込み前のプレビュー（オーバーレイで見せてから確定）— 未着手。現状はモードAのオーバーレイ表示自体が実質的なプレビューを兼ねている
- [ ] 閲覧専用スライドでの無効化 — 未着手（batchUpdate が権限エラーを返した場合のハンドリングを別途追加する）

**実機テストで発見・修正した不具合（2026-09-17）**:
- `getPageContainerElement` のセレクタが誤っていた。Phase 0 では固定の `id="editor-p"` を前提にしていたが、実際のプレゼンテーションでは `editor-p<スライドのobjectId>`(例: `editor-p1`)という形式で、Phase 0 の検証が新規作成した単一ページの特殊ケースだったために気づけなかった。`src/content/selectors.ts` を修正
  - **(2026-09-26 追記・v0.6.1 で再修正)** 上の理解もまだ不正確だった。正しくはページルートの id は **`editor-` + スライドの objectId**(`p1` → `editor-p1`)。複製・コピー・取り込みで作られたスライドは objectId が `h273037817fe35b37_0_0` のような形になり、ルートも `editor-h273037817fe35b37_0_0` になる。`editor-p` で始まり `-`/`_` を含まない id だけを探していたため、コピーしたプレゼンテーションで「スライドの表示領域が見つかりませんでした」になった(ユーザー報告)。id の形に頼らず「`editor-` で始まり、祖先に `editor-` の id を持たない要素」を候補にし、選び方は `choosePageRoot`(単体テストあり)に切り出した。あわせて、id 照合が `editor-p` + objectId(`editor-pp1`)になっていて一度も一致していなかったため、`navigateToSlide` の切り替え完了待ちが常に即時成功していた不具合も修正(`findPageRootSvgById` で厳密一致のみ)。**教訓: 実機確認は新規作成したスライドだけでなく、複製・コピーしたスライドでも行う**
  - **(2026-09-26 追記・v0.7.0 で修正)** 書き込んだルビが左上ほど大きく左に、下の行ほど下にずれ、文字も小さくなる不具合(ユーザー報告)。px→EMU 換算の基準にしていたページルートの `<svg>` が、スライド本体ではなく周りのグレーの余白を含む編集領域全体の大きさを持っていた(実測: svg 1145×531px・縦横比 2.156 に対し、スライド本体 884×497px・16:9)。横と縦で別々の倍率で換算がずれていた。スライド本体の枠はページ背景の要素 `editor-<objectId>-bg` が正確に持つので、`getPageContainerElement` はそれを返すように修正(見つからない場合のみ svg)。**教訓: 換算の基準にする要素は、縦横比がスライドの縦横比と一致するかで確かめる**
  - **(2026-09-26 追記・v0.7.0 で修正)** 上を直しても、ルビがどの行でも一律に約25px下にずれた(ユーザー報告)。書き込み後の位置補正(`recenterByDom`)が、書き込み前に測ったビューポート座標のまま比べていたため。書き込み後に画面下部へ「このスライドをブラッシュアップ」の案内が出ると、編集領域が縮んでスライドが上に動く(実測: 案内を閉じるとスライドの上端が 228→252px、24px 動いた)。正しく置かれたルビを「ずれている」と誤判定し、動いた分だけ逆にずらしていた。意図した位置は測ったときのスライドの枠と一緒に持ち(`pageAtMeasurePx`)、補正のたびに枠を測り直してスライドに対する相対位置で比べるよう修正(`remapRectBetweenFrames`)。文字の位置とスライドの枠も、段落ごとに await を挟まず続けて測る。**教訓: 画面上の座標は、比べる直前に基準の枠と一緒に測り直す**
- Slides API の `updateShapeProperties` リクエストで `autofit` フィールドを更新しようとしてエラーになった(`autofit` は読み取り専用フィールド)。`src/core/slidesRequests.ts` から削除して解決

完了条件: 書き込んだルビが共有相手の画面と PDF 書き出しに現れ、一括削除で元に戻る。**✅ 達成（2026-09-17）**。ユーザーの実 Chrome 環境・実 GCP プロジェクトで OAuth 認可から書き込み・削除、および「全スライド」への一括書き込みまで一連の流れを実機確認済み。

**「全スライド」実装時に発見・修正した不具合（2026-09-17）**:
- スライドを切り替えると、以前表示していたページの SVG ルートが `display:none` のまま DOM に残り続け、複数ページのルートが同時に存在する状態になる。`getPageContainerElement`(`src/content/selectors.ts`)が単純に最初のマッチを返す実装だったため、誤ったページを対象に測定してしまう恐れがあった。`pageObjectId` を渡した場合は厳密一致、渡さない場合は実際にレンダリングされている(サイズを持つ)ものを優先するよう修正
- フィルムストリップのサムネイルが表示範囲外にスクロールされている(例: `clientY` が負の値)状態で合成クリックを発行しても、Google 側のクリックハンドラに無視されページ切り替えが発生しない。クリック前に `scrollIntoView()` でフィルムストリップ内へ実際にスクロールさせることで解決。遠距離ジャンプ(最終ページ→先頭ページ等)でのスクロールアニメーション遅延に備え、`navigateToSlide` に最大3回のリトライも追加

**ルビ書き込み後にモード A(画面表示)を自動 OFF にする機能追加時、実機テストで発見・修正した位置ズレの不具合（2026-09-17）**:
書き込んだルビと本文が重なって表示される不具合をユーザーから指摘され調査した結果、2つの根本原因が判明:
- `writeController.ts` が `computeParagraphPlan` の返す `RubyPlacement.box`(本文文字そのものの矩形)を、書き込むシェイプの矩形として**そのまま**使っていた。モード A の `overlayRenderer.ts`(`createRubySpan`)は同じ `box` から独自に「本文の少し上」へ座標をずらして描画していたが、モード B にはその変換が存在せず、書き込んだシェイプが本文とほぼ同じ位置に配置されてしまっていた。`src/content/rubyLayout.ts` に `computeRubyBoxAboveBody`(本文の矩形から、ルビ自身のフォントサイズ・文字数をもとに「本文の直上・1行で折り返さない幅」のボックスを計算する純粋関数)を追加し、`writeController.ts` で EMU 変換前に適用するよう修正
- 上記を修正してもなお、5文字程度の長い読み(「まーじゃん」等)がシェイプの右端で折り返され、2行になってしまうケースが残った。原因は Slides API で新規作成したテキストボックスが持つ**既定の内部余白(インセット、PowerPoint/OOXML 互換の左右 0.1inch・上下 0.05inch)**で、ぴったりのサイズでシェイプを作ると内側の実効幅がその分狭くなり折り返される。Slides API はインセットを直接設定するフィールドを公開していないため、`src/core/emu.ts` に `expandRectForDefaultInsets`(想定インセット分だけ矩形を外側に広げ、内側の実効サイズを相殺する純粋関数)を追加し、EMU 変換後に適用することで解決
- 上記2つを修正してもなお、「作って」の「作」に振った「つく」等、一部の単語でルビが本文から水平方向に数十px単位でずれる不具合が残った。`presentations.get` でシェイプを読み直すと位置・サイズは指定どおりだったため当初は原因不明だったが、生の API レスポンスを調査した結果、**Slides は新規テキストボックスの内部座標を常に 3,000,000EMU四方の基準矩形に正規化し、`transform.scaleX`/`scaleY` で実効サイズを表現している**ことが判明(例: 幅広・薄いルビ用ボックスは `scaleX≈0.62, scaleY≈0.16` のように縦横で大きく異なる倍率になる)。この非一様スケーリングが、シェイプ内部の CENTER 揃えテキストの実際の描画位置に影響しており、Slides API 側では検出も補正もできない。そこで方針を変更し、**書き込み直後に実際に画面上へレンダリングされたルビの位置を DOM から実測し、意図していた位置とのズレを `updatePageElementTransform`(RELATIVE)で補正する**方式にした(`writeController.ts` の `recenterByDom`)。1回の補正では残差(実測で最大 8px 程度)が残ることがあったため、ズレが十分小さくなるか上限回数(3回)に達するまで測定→補正を繰り返す設計にし、実機で残差 1px 未満まで収束することを確認済み

### Phase 3: 品質向上と配布（1〜2 週間）

- [x] ユーザー辞書のデータ構造を拡張（3.7節）: `Record<string, string>` → `Record<string, UserDictEntry>`（読み + フォント/色/サイズの個別上書き）✅ 完了（2026-09-18）。`core/{types,reading,userDict}.ts`・`shared/userDictStorage.ts`・`content/overlayRenderer.ts` を更新。加えて、辞書の表層形がトークン境界と一致しない場合（実機で発見: 「雀魂」がkuromoji上「雀」「魂」に分かれる等）に備え、`core/reading.ts` の `mergeUserDictTokens` でトークンを辞書語の境界に合わせて組み直す処理を追加した
- [x] ユーザー辞書の options ページ（辞書エントリの追加・編集・削除、読みと見た目の個別設定、インポート / エクスポート）✅ 完了（2026-09-18）。`src/options/{index.html,main.ts}`
- [x] ルビの全体設定にフォント・色を追加 ✅ 完了（2026-09-18）。当初 popup に実装したが、下記の理由でスライド編集画面内のフローティングパネルに置き換えた
- [x] 設定 UI を popup からフローティングパネルへ置き換え ✅ 完了（2026-09-18、v0.5.0）。**理由**: Chrome 拡張のツールバー popup は仕様上ブラウザがアイコン直下に固定表示するもので、Web側のコードからは移動できない。ユーザーから「パネルを自由に移動できるようにしてほしい」との要望を受け、`action.default_popup` を廃止し、アイコンクリック(`chrome.action.onClicked` → service worker → content script への `rubi/toggle-panel` メッセージ)でスライド編集画面内にドラッグ移動可能なパネル（`src/content/panel.ts`、Shadow DOM）を開閉する方式に変更した。位置は `localStorage` に記憶する。書き込み・削除操作は同じ content script プロセス内で動くため、popup 時代のような `chrome.tabs.sendMessage` 中継が不要になった
- [x] 設定パネルをフローティングパネルから Chrome のサイドパネルへ置き換え(2026-09-26、v0.7.0)。**理由**: ユーザーから「右サイドバー固定にしてスライド全体が常に見えるようにしてほしい」との要望。ページ内のパネルを右端に固定するだけでは、Googleスライドは画面幅いっぱいでレイアウトしたままなのでスライドの右側が隠れる。Chrome のサイドパネル(`chrome.sidePanel`、`side_panel.default_path` = `src/sidepanel/index.html`)はページの表示幅そのものを狭めるため、Googleスライドがその幅に合わせてレイアウトし直し、スライド全体が見える。アイコンクリックで開く(`setPanelBehavior({ openPanelOnActionClick: true })`)。ドラッグ移動はなくなった。書き込み・削除はスライドの DOM を測る必要があるため、サイドパネルから開いているタブの content script に `rubi/panel-command` で依頼する(`src/content/panelBridge.ts`)。サイドパネルでは `window.confirm()` が使えない場合があるため、「全スライド」の確認はパネル内に表示する
- [x] ルビのフォント選択肢を拡充 ✅ 完了（2026-09-18）。当初 5 種（Arial 等の定番のみ）だったが「選べるフォントが少ない、最低限 Google スライドに含まれるフォントは選べるように」との要望を受け、Google スライドの標準フォント一覧＋日本語 Web フォントを追加（`src/shared/fontOptions.ts`、`FONT_GROUPS` でグループ分け）。**実機で発見した不具合**: 追加した Web フォント（Google Fonts）は、そのスライド上でまだ使われていない場合ブラウザにフォントファイルが読み込まれておらず、指定しても既定フォントのまま変化しなかった（`document.fonts.check()` は `true` を返すが、canvas 実測幅で確認すると実在しないフォント名と同じ幅だった＝実際には代替フォントで描画）。`src/content/webFontLoader.ts` で選択されたフォントを Google Fonts の stylesheet から動的に読み込むことで解消（OS標準の定番フォントは対象外）。この問題はモード A（画面表示の自前 `<span>` 描画）のみで発生し、モード B（Slides API 書き込み）は Slides 自身がフォントを読み込むため影響なし
- [x] ルビのサイズ指定を「小・中・大」の3段階に単純化 ✅ 完了（2026-09-18）。当初 30〜100% の連続スライダーだったが「30%〜100%がわかりにくい、小中大の3種でいいのでは」との要望を受け、`src/shared/sizePresets.ts` の3プリセット（35% / 50% / 65%）から選ぶ UI に変更（内部的には引き続き sizeRatio 数値として保存）。**実機で発見した特性**: 読みの文字数が多い単語は、隣接ルビとの重なりを防ぐための幅ベースの上限（`rubyLayout.ts` の `widthSafetyFactor`）に張り付き、小・中・大を切り替えても見た目がほぼ変わらないことがある。ロジック自体は変更せず（既存の重なり防止策のため）、UI 側で対処: サイズ変更後に実際の描画結果を比較し、変化がなければボタン付近に一言添える動的ヒント（例:「これ以上大きくできません」）を `panel.ts` に実装した
- [ ] 表・グループ・回転テキストへの対応
- [ ] エラー時のフェイルセーフと通知
- [ ] i18n（ja / en）
- [ ] プライバシーポリシー、ストア掲載文、スクリーンショット
- [ ] Chrome ウェブストア公開（限定公開 → 一般公開）

### 概算スケジュール

| Phase | 期間 | 累計 |
|---|---|---|
| 0 技術検証 | 1 週 | 1 週 |
| 1 表示 MVP | 2〜3 週 | 3〜4 週 |
| 2 書き込み | 2〜3 週 | 5〜7 週 |
| 3 品質・配布 | 1〜2 週 | 6〜9 週 |

1 人で開発する前提。OAuth 審査はこの表の外で並行して進める。

---

## 6. ディレクトリ構成（案）

```
rubi/
├── manifest.json
├── package.json
├── vite.config.ts
├── src/
│   ├── content/
│   │   ├── index.ts          # エントリ、DomWatcher 起動
│   │   ├── selectors.ts      # Slides の DOM セレクタを集約
│   │   ├── textExtractor.ts
│   │   ├── overlayRenderer.ts
│   │   ├── coordMapper.ts    # Phase 2
│   │   └── injector.ts       # Phase 2
│   ├── worker/
│   │   ├── index.ts          # メッセージルーティング
│   │   ├── tokenizer.ts      # kuromoji ラッパと loader 差し替え
│   │   ├── slidesClient.ts   # Phase 2
│   │   └── auth.ts           # Phase 2
│   ├── core/                 # DOM 非依存。単体テストの主対象
│   │   ├── reading.ts        # 送り仮名分離、区間割り当て
│   │   ├── gradeFilter.ts
│   │   ├── userDict.ts
│   │   └── types.ts
│   ├── popup/
│   └── options/
├── public/
│   ├── dict/                 # kuromoji 辞書
│   └── data/kanji-grades.json
└── tests/
    ├── unit/
    └── e2e/fixtures/slides-like.html
```

---

## 7. 決定事項（2026-09-17 時点）

1. **モード**: A（表示オーバーレイ）と B（スライドへの書き込み）の両方を実装する。Phase 2 は必須スコープ。
2. **配布形態**: Chrome ウェブストアで一般公開する。`presentations` scope は sensitive scope のため OAuth 審査が必須。Phase 0 開始と同時に GCP プロジェクト作成と審査申請を進め、開発と並行させる（Phase 2 の着手を審査待ちで止めない）。
3. **学年フィルタの既定値**: 「フィルタなし（すべての漢字にルビを付ける）」を既定とする。対象読者を教材利用に限定せず、日本語学習者や漢字が苦手な大人も含めた最大公約数のユースケースを既定にする。学年別フィルタは options ページの詳細設定として提供し、教材用途の人はそこで対象学年を選ぶ。
4. **ルビの単位**: 熟語・単語単位のグループルビを既定とする。モノルビ（1 文字ずつの割り当て）は Phase 3 以降、設定で選べる追加オプションとして扱う。

### 決定を受けた計画への反映

- ウェブストア公開が確定したため、OAuth 同意画面の準備（プライバシーポリシー URL、スコープの利用目的説明、テスト用アカウントでの審査提出）を **Phase 0 と並行して着手**する。審査には数週間かかるため、Phase 2 の開発完了を待たずに先に提出する。
- 学年フィルタは既定 OFF（フィルタなし）で実装するため、Phase 1 の ReadingService は「学年データを引かない」パスを主経路とし、学年データの参照はオプトインの分岐として実装する。
- グループルビが既定のため、Phase 1 の OverlayRenderer と Phase 2 の Injector はいずれも「区間（トークン）単位でひとまとまりの読みを描画・書き込みする」ことを主経路として設計する。モノルビ用の 1 文字ずつのレイアウトは Phase 3 で追加する拡張点として core 層のインターフェースに余地を残す（`rubyRanges` は最初から区間配列として設計済みのため、モノルビは区間を文字数まで分割するだけで対応可能）。
